name: ci.yml
on:
  pull_request:
    branches:
      - main
      - develop
#    paths:
#      - "api/**"
#      - "admin/**"
#      - "batch/**"
#      - ".github/workflows/**"
#      - "docker-compose.yml"

jobs:
  ci:
    runs-on: ubuntu-latest

#    services:
#      mysql:
#        image: mysql:8.0
#        env:
#          MYSQL_ROOT_PASSWORD: root
#          MYSQL_DATABASE: quick_stack
#        ports:
#          - 3306:3306
#        options: >-
#          --health-cmd="mysqladmin ping"
#          --health-interval=10s
#          --health-timeout=5s
#          --health-retries=3
    steps:

      - name: checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Gradle 권한 부여
        run: chmod +x gradlew

#      - name: 변경 모듈 감지 , 테스트 /빌드
#        run: |
#          CHANGED=$(git diff --name-only origin/${{ github.base_ref }} HEAD)
#          echo "$CHANGED"
#
#          if echo "$CHANGED" | grep -q "^api/"; then
#            ./gradlew :api:test
#          fi
#
#          if echo "$CHANGED" | grep -q "^batch/"; then
#            ./gradlew :batch:test
#          fi
#
#          if echo "$CHANGED" | grep -q "^admin/"; then
#            ./gradlew :admin:test
#          fi