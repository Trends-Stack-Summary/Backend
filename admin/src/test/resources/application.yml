spring:
  application:
    name: admin
  datasource:
    username: root
    password: root
---
spring:
  config:
    activate:
      on-profile: test

  datasource:
    url: jdbc:mysql://localhost:3306/quick_stack
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update

gemini:
  api-key: 123456
